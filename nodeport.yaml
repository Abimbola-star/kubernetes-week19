apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  labels:
    app: nginx
spec:
  type: NodePort
  #type: LoadBalancer
  #type: ClusterIP
  selector:
    app: app1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80